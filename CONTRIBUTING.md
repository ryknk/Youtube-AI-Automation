# CONTRIBUTING

Git運用・ブランチ運用のルール。

---

## 基本方針

- commit・push・ブランチ作成は、ユーザーが明示的に指示した場合のみ行う（[CLAUDE.md](CLAUDE.md)のGit節）。
- 未コミットの変更を勝手に破棄しない。作業前に`git status`で既存の変更内容を確認する。
- コミット前に関連するテストを実行し、`pytest`が通ることを確認する。

## ブランチ運用

現状、このリポジトリは`main`ブランチへの直接コミットのみで運用されており、フィーチャーブランチ・PRベースの運用は確立されていない（`git branch -a`で`main`のみ、`.github/`ディレクトリなし）。

- 新たにブランチ運用・PRベースのワークフローを導入する場合は、着手前にユーザーへ方針を確認する。推測で新しいブランチ戦略を導入しない。

## コミット単位

- 1コミットは1つの機能または1つの不具合修正に対応させる（[CLAUDE.md](CLAUDE.md)の開発方針「1回の作業では、原則として1つの機能または1つの不具合だけ扱う」に対応）。
- 無関係な変更（フォーマット崩れの修正など）を同じコミットに混ぜない。

## コミットメッセージ規約

既存のコミット履歴（`git log`）で観測された規約に従う。

- 日本語、1行、体言止め（例: `動画レベルキャッシュの実装`、`キャッシュの過剰無効化を修正`、`テンプレートごとのVOICEVOXの設定に対応`）。
- `feat:`/`fix:`のようなConventional Commits形式のプレフィックスは使用しない。
- 「〜を実装」「〜を修正」「〜に対応」「〜を追加」など、変更内容が一目でわかる動詞で締める。
- 本文（1行目以降の詳細説明）は現状のコミット履歴では使われていない。必要な背景・理由は[DECISIONS.md](DECISIONS.md)へ残す。

## コミット前チェックリスト

1. `git status`で意図しない変更・未追跡ファイルが含まれていないか確認する。
2. `.env`、`client_secret.json`、`youtube_token.json`などの秘匿情報を含むファイルをステージしていないか確認する（`.gitignore`で除外済みだが、誤って追加していないか二重確認する）。
3. `python -m pytest`を実行し、全テストが通ることを確認する（外部APIテストは通常実行では自動スキップされる）。
4. 変更内容がCLAUDE.md・DECISIONS.md・TASKS.mdの記載と矛盾していないか確認し、矛盾があれば併せて更新する。

## 実装作業の進め方

[CLAUDE.md](CLAUDE.md)の「実装前」「実装後」の手順に従う。

- 実装前: 関連コードを読み、データフローと影響範囲を確認する。
- 実装後: 変更ファイル・実装内容・テスト結果・既存機能への影響・未確認事項を必ず報告する。推測で「動作する」と報告しない。

---

## 関連ドキュメント

- [CLAUDE.md](CLAUDE.md) — 開発方針・アーキテクチャ・コーディング規約
- [TASKS.md](TASKS.md) — 今後実装予定のタスク一覧
- [DECISIONS.md](DECISIONS.md) — 技術選定の理由・設計意図
